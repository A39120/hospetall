<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.hospetall.ps.isel.hospetallmobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".HospetallApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PetActivity"/>
        <activity android:name=".activities.PetsListActivity" />
        <activity android:name=".activities.ConsultationActivity"/>
        <activity android:name=".activities.TreatmentActivity"/>
        <activity android:name=".activities.ConsultationListActivity"/>
        <activity android:name=".activities.TreatmentListActivity"/>
        <activity android:name=".activities.ProfileActivity"/>
    </application>

</manifest>