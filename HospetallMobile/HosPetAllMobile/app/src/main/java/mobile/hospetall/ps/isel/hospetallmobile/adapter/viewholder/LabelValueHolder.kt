package mobile.hospetall.ps.isel.hospetallmobile.adapter.viewholder
/*
import android.support.v7.widget.RecyclerView
import android.view.View
import android.widget.RelativeLayout
import android.widget.TextView
import mobile.hospetall.ps.isel.hospetallmobile.R
import mobile.hospetall.ps.isel.hospetallmobile.databinding.LayoutLabelValueHorizontalBinding

class LabelValueHolder:  RecyclerView.ViewHolder {

    private val hbinder : LayoutLabelValueHorizontalBinding?

    constructor(binder : LayoutLabelValueHorizontalBinding) : super(binder.root) {
        this.hbinder = binder
    }

}

    val container: RelativeLayout
    val label: TextView
    val value: TextView

    constructor(itemView: View) : super(itemView) {
        container = itemView as RelativeLayout
        label = itemView.findViewById(R.id.label)
        value = itemView.findViewById(R.id.value)
    }

    fun setLabelValue(label: String, value: String?) =
            value?.let {
                this.label.text = label
                this.value.text = it
                this.container.visibility = View.VISIBLE
            }
}
*/

